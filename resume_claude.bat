@echo off
:: Resume the cfg_dev_environment Claude Code session.
:: Run this from anywhere — it changes to the repo directory automatically.
cd /d "%USERPROFILE%\projects\personal\cfg_dev_environment"
claude --resume cfg_dev_environment