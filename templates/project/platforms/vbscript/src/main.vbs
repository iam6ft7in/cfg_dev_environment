Option Explicit
' {{REPO_NAME}}, {{DESCRIPTION}}
' Platform: VBScript
' Dependencies: See docs/dependencies.md

' Import helpers (VBScript doesn't have native imports;
' use ExecuteFile or concatenate files at build time)

Sub Main()
    WScript.Echo "{{REPO_NAME}} starting..."
End Sub

Main
