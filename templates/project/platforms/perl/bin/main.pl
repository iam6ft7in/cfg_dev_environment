#!/usr/bin/env perl
# {{REPO_NAME}}, {{DESCRIPTION}}
# Platform: Perl

use strict;
use warnings;
use utf8;
use FindBin qw($RealBin);
use lib "$RealBin/../lib";

use ProjectName::Helpers;

sub main {
    print "{{REPO_NAME}} starting...\n";
}

main();
